const data = new Date();

console.log(data.getDay()); // retorna o dia da semana de uma data.

console.log(data.getMonth()); // retorna o número do mês de uma data.

console.log(data.getUTCFullYear()); // retorna o ano completo de uma data.

console.log(data.getDate()); // retorna o número do dia do mês de uma data.

console.log(data);
